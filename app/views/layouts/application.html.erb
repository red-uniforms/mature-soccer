<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>Mad for Football</title>

  <!-- Mobile Specific Metas
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default">
    <div class="container">
      <a class="navbar-link" href="/" id="logo" style=""></a>
      <a class="navbar-link" target="_blank" href="http://enccer.com">Enccer</a>
      <% if current_user %>
        <%= link_to "log out", destroy_user_session_path, class: "navbar-link u-pull-right", method: :delete, data: { confirm: "Sure to Log Out?"} %>
        <span class="navbar-text"><%= current_user.name %> 님</span>
      <% else %>
        <%= link_to "log in", new_user_session_path, class: "navbar-link u-pull-right" %>
      <% end %>

    </div>
  </nav>

  <%= yield %>

  <footer>
    <div class="container">
      <div class="three columns">
        <div class="row">
          <%= image_tag('logo_MFF_white.png', class: "icon-big", alt: "madforfootball logo") %>
        </div>
        <div class="row">
          <p>© 2015-2018, MadForFootball.</p>
        </div>
      </div>
    </div>
  </footer>

</body>
</html>
