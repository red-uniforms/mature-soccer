<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Mad for Football</title>

  <!-- Mobile Specific Metas
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="login">
  <section>
    <div class="container">

      <div class="six columns offset-by-three login-panel">
        <div class="row">
          <div class="u-full-width" id="logo-big">
          </div>
        </div>
        <div class="row">
          <%= link_to "Login", new_user_session_path, class: "login-nav u-pull-left" %>
          <%= link_to "Signup", new_user_registration_path, class: "login-nav u-pull-left" %>
          <%= link_to "PASSWORD?", new_user_password_path, class: "login-nav u-pull-right" %>
        </div>
        <%= yield %>
      </div>

    </div>
  </section>
</body>
</html>
